# Homebrew services — run formulae as background daemons via launchctl
tap "homebrew/services"
# Official tap for the Bun JavaScript runtime
tap "oven-sh/bun"

# AWS command-line interface for managing cloud resources
brew "awscli"
# iOS/macOS dependency manager for Cocoa projects
brew "cocoapods"
# Generates reproducible lockfiles for conda environments
brew "conda-lock"
# Prints messages in an ASCII art speech bubble (fun CLI utility)
brew "cowsay"
# Docker CLI for building and running containers
brew "docker"
# Sets default applications for file extensions, URLs, and UTI handlers
brew "duti"
# Modern `ls` replacement with icons, git status, and tree view
brew "eza"
# Automates iOS and Android app builds, signing, and deployment
brew "fastlane"
# Fast, user-friendly alternative to `find`
brew "fd"
# Creates thumbnail images from video files via FFmpeg
brew "ffmpegthumbnailer"
# Fast Node Manager — installs and switches Node.js versions
brew "fnm"
# Interactive fuzzy finder for files, history, and command-line input
brew "fzf"
# GitHub CLI for repos, issues, PRs, and Actions from the terminal
brew "gh"
# Go programming language toolchain
brew "go"
# Graph visualization tools (`dot`, `neato`, etc.) for diagrams and dependency graphs
brew "graphviz"
# # Modern Python project manager for packaging, environments, and publishing
# brew "hatch"
# Interactive process viewer with CPU, memory, and per-process stats
brew "htop"
# Image manipulation suite for resize, convert, and batch processing
brew "imagemagick"
# Command-line JSON parser, filter, and formatter
brew "jq"
# # IPFS node implementation (formerly go-ipfs) for decentralized storage
# brew "kubo"
# Terminal UI for git — staging, commits, branches, and diffs
brew "lazygit"
# Lists USB devices connected to the system
brew "lsusb"
# GNU Make build automation tool
brew "make"
# Creates locally trusted TLS certificates for HTTPS development
brew "mkcert"
# Lightweight, scriptable media player for video and audio
brew "mpv"
# Displays system info (OS, kernel, CPU, memory) in the terminal
brew "neofetch"
# # CLI for Neon serverless Postgres — branches, databases, and connection strings
# brew "neonctl"
# Extensible Vim-based text editor with LSP and plugin ecosystem
brew "neovim"
# Picks the right Node package manager (npm/yarn/pnpm/bun) from lockfiles
brew "ni"
# Runs large language models locally on your machine
brew "ollama"
# OpenJDK — Java runtime and development kit
brew "openjdk"
# Reports CPU temperature from the command line on macOS
brew "osx-cpu-temp"
# Bun — fast JavaScript/TypeScript runtime and all-in-one toolkit
brew "oven-sh/bun/bun"
# # macOS PIN entry dialog for GPG and SSH key passphrases
# brew "pinentry-mac"
# Installs Python CLI tools in isolated, upgradeable virtual environments
brew "pipx"
# # Fast conda-like package manager for Python, R, and other scientific stacks
# brew "pixi"
# # PDF utilities — extract text, convert pages, merge/split documents
# brew "poppler"
# # Cross-platform audio I/O library for recording and playback in apps
# brew "portaudio"
# # Framework for managing and running git pre-commit hooks
# brew "pre-commit"
# Efficient file sync and transfer over local or remote paths
brew "rsync"
# Rust programming language toolchain (rustc, cargo)
brew "rust"
# # Swiss Army knife for audio — convert, trim, effects, and analysis
# brew "sox"
# Minimal, fast, cross-shell prompt with git and language indicators
brew "starship"
# Terminal multiplexer — persistent sessions, splits, and remote attach
brew "tmux"
# Extracts archives (RAR, 7z, tar, etc.) from the command line
brew "unar"
# File-watching service used by React Native, Metro, and other dev tools
brew "watchman"
# Non-interactive file downloader for HTTP, HTTPS, and FTP
brew "wget"
# # Terminal file manager with preview, search, and async I/O
# brew "yazi"
# Scans GitHub Actions workflows for security misconfigurations
brew "zizmor"
# Remembers frequently used directories for smarter `cd` navigation
brew "zoxide"

# Virtual 2-channel audio driver for routing audio between apps (e.g. OBS, DAWs)
cask "blackhole-2ch"
# Anthropic's agentic coding assistant for the terminal
cask "claude-code"
# OpenAI Codex app for AI-assisted coding
cask "codex"
# Nerd Font icon glyphs for terminal prompts and dev tools
cask "font-symbols-only-nerd-font"
# GPU-accelerated terminal emulator with native macOS integration
cask "ghostty"
# macOS automation with Lua — window management, hotkeys, and scripting
cask "hammerspoon"
# Lightweight, customizable Discord client
cask "legcord"
# # FUSE file system layer for mounting non-native volumes on macOS
# cask "macfuse"
# # Minimal Conda installer for Python data-science environments
# cask "miniconda"
# Controls external monitor brightness and volume over DDC/CI
cask "monitorcontrol"
# Fast Docker and Linux VM runtime for macOS (Docker Desktop alternative)
cask "orbstack"
# Menu bar system monitor — CPU, memory, disk, network, and sensors
cask "stats"
# Minimal GUI for running AI code agents locally
cask "t3-code"
# # Modern terminal with blocks, workflows, and AI-assisted command line
# cask "warp"
# # X11 window system for running X11-based Linux apps on macOS
# cask "xquartz"
